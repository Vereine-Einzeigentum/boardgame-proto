# Browser-visible endpoint for the realtime room authority.
NEXT_PUBLIC_GAME_SERVER_URL=http://localhost:3001

# Port and comma-separated browser origins accepted by the Node authority.
GAME_PORT=3001
CLIENT_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Server-only. Enables full handle resolution, deeper upload catalogs, and
# filtering for public/embeddable videos. Never expose this in GitHub Pages.
YOUTUBE_API_KEY=
